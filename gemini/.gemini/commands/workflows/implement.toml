description = "Acione o `staff-engineer-orchestrator` para analisar e implementar a demanda descrita abaixo."
prompt = """
Acione o `@{agents/staff-engineer-orchestrator.md}` para analisar e implementar a demanda descrita abaixo.

## Regras
- NUNCA implemente sem análise adequada
- Siga a ordem de consulta de agentes definida no GEMINI.md
- Se houver dependências (pom.xml, build.gradle, go.mod, pyproject.toml, Terraform providers), o `@{agents/dependency-versions-reviewer.md}` DEVE ser acionado antes do `@{agents/software-engineer.md}`
- Entregue a menor implementação correta, sustentável e profissional

## Demanda
$ARGUMENTS

"""
