# Prompt Files (Copilot)

Prompts sincronizados de `base de conhecimento importada` para uso no Copilot Chat.

- Use no chat com `/nome-do-prompt`.
- Substitua `{{ARGUMENTS}}` pelo contexto da tarefa.

